import React from 'react';

const RestartButton = props => (
    <div id="restart">
        <button onClick {...props}>
            Restart
        </button>
    </div>
);

export default RestartButton;
